{
  "name": "agent-social",
  "version": "1.0.0",
  "description": "Giving AI Agents a social identity: Unified bridge for notifications and commands via Feishu, DingTalk, and more.",
  "main": "dist/src/main.js",
  "bin": {
    "agent-social": "dist/src/main.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "dev:reg": "ts-node src/main.ts register",
    "dev:run": "ts-node src/main.ts run",
    "start": "node dist/src/main.js",
    "lint": "tsc --noEmit",
    "build": "npm run lint && tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "feishu",
    "lark",
    "bot",
    "gemini",
    "agent"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.58.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}